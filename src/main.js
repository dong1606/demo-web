import { createApp } from 'vue'
import './style.css'
import App from './App.vue'

createApp(App).mount('#app')

let bien_chua_dung = "Biến này tạo ra cho vui chứ không dùng";
console.log(mat_khau_database);
